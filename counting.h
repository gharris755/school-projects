#ifndef COUNTING_H
#define COUNTING_H



#include <ctime>
#include <cmath>
#include <cstdlib>
//header file that allows the time functions to be called when needed to record during sorting.

time_t getTime();
double totalTime(time_t start, time_t end);



#endif